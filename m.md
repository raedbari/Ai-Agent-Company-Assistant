النتيجة صحيحة. النموذج فهم أن الموقع موجود، ولخّص الحماية واللغة دون قواعد كلمات. هذه نقطة توقف واضحة نسميها:

## نقطة التحقق: نواة موظف خدمة العملاء

```text
Travel-X Conversational Ticket MVP — النسخة 0.1
```

التقدم التقريبي:

* نواة المحادثة والتذاكر: نحو 75%.
* النظام الإنتاجي الكامل: نحو 35%.

## ما يعمل الآن؟

```text
العميل
→ واجهة FastAPI
→ سياسة الدخول
→ Router
→ فهم وتلخيص النموذج
→ Evidence Guard
→ جمع المتطلبات
→ إنشاء مسودة
→ تعديل المسودة
→ تأكيد نسخة محددة
→ إنشاء تذكرة دون تكرار
→ رقم وحالة وسجل إنشاء
```

| الجزء                                  | حالته                | ما يفعله                               |
| -------------------------------------- | -------------------- | -------------------------------------- |
| واجهة المحادثة (`API`)                 | مكتمل للنسخة الأولية | تستقبل الرسائل وتحفظ رقم الجلسة        |
| ربط النموذج (`LangChain`)              | مكتمل                | يربط (OpenRouter) بالمخرجات المنظمة    |
| المخرجات المنظمة (`Structured Output`) | مكتمل                | يمنع التعامل مع رد النموذج كنص عشوائي  |
| الموجّه (`Router`)                     | مكتمل مبدئيًا        | يحدد النية والخدمة والمسار             |
| المسار المخصص (`LangGraph Workflow`)   | مكتمل مبدئيًا        | ينظم مراحل المحادثة                    |
| كتالوج الخدمات                         | مكتمل مبدئيًا        | يحدد الخدمات والأقسام والمتطلبات       |
| جمع المتطلبات                          | مكتمل                | يجمع الإجابات ويحفظها                  |
| الفهم الدلالي                          | مكتمل مبدئيًا        | يفهم اللهجة ويلخص المتطلب              |
| حارس الأدلة (`Evidence Guard`)         | مكتمل                | يمنع نقل معلومات غير موجودة في الرسالة |
| سياسة السعر                            | مكتملة               | تمنع إعطاء سعر نهائي                   |
| المسودة                                | مكتملة               | تعرض المتطلبات قبل الإنشاء             |
| تعديل المسودة                          | مكتمل مبدئيًا        | يقبل أو يرفض تعديلات العميل دلاليًا    |
| تأكيد النسخة                           | مكتمل                | لا يقبل نسخة قديمة                     |
| منع التكرار (`Idempotency`)            | مكتمل في الذاكرة     | يمنع إنشاء تذكرتين من التأكيد نفسه     |
| سجل التدقيق                            | جزئي                 | يسجل إنشاء التذكرة فقط                 |
| الاختبارات                             | 32 اختبارًا ناجحًا   | تحمي المسارات الأساسية                 |

## ما هو مؤقت أو غير مكتمل؟

| الجزء                 | الوضع الحالي               | المطلوب إنتاجيًا                |
| --------------------- | -------------------------- | ------------------------------- |
| الجلسات               | داخل الذاكرة               | قاعدة بيانات دائمة              |
| التذاكر               | داخل الذاكرة               | تخزين في (`PostgreSQL`)         |
| إرسال التذكرة للقسم   | اسم القسم داخل التذكرة فقط | ربط (`CRM`) أو نظام تذاكر       |
| متابعة الحالة         | النية موجودة فقط           | قراءة الحالة من النظام الحقيقي  |
| إلغاء التذكرة         | غير منفذ                   | مسار إلغاء مع صلاحيات           |
| الرسائل المتكررة      | العدادات موجودة فقط        | حساب التكرار فعليًا             |
| الإيقاف المؤقت        | السياسة موجودة جزئيًا      | تخزين المدة ومنع الطلبات فعليًا |
| التحقق البشري         | غير مربوط                  | (`CAPTCHA`) أو نقطة تحقق        |
| الموظف البشري         | غير مربوط                  | (`Handoff`) حقيقي               |
| معرفة خدمات الشركة    | كتالوج ثابت                | قاعدة معرفة (`RAG`)             |
| الملفات والمرفقات     | غير موجودة                 | رفع وفحص وتخزين الملفات         |
| المراقبة              | اختبارات فقط               | (`LangSmith`) وسجلات تشغيل      |
| واجهة الموقع          | غير موجودة                 | (`Widget`) للمحادثة             |
| المستخدمون والصلاحيات | غير موجودة                 | مصادقة وصلاحيات                 |
| النشر                 | غير منفذ                   | حاويات وبيئة إنتاج              |

## الأنماط المستخدمة حاليًا

| النمط                    | هل استخدمناه؟ | مكانه                                |
| ------------------------ | ------------: | ------------------------------------ |
| (`Router`)               |           نعم | تحديد النية والخدمة ومسار الرسم      |
| (`Custom Workflow`)      |           نعم | مراحل الجمع والمراجعة والتأكيد       |
| (`Structured Output`)    |           نعم | قرارات النموذج المنظمة               |
| (`Memory`)               |    نعم مؤقتًا | حالة المحادثة في الذاكرة             |
| (`Guardrails`)           |           نعم | الدليل والثقة والسعر والمفاتيح       |
| (`Human Confirmation`)   |           نعم | العميل يؤكد المسودة                  |
| (`Port/Adapter`)         |           نعم | فصل إنشاء التذكرة عن التخزين         |
| (`Idempotency`)          |           نعم | منع التذاكر المكررة                  |
| (`Audit Event`)          |          جزئي | حدث إنشاء التذكرة                    |
| (`Tool Calling`)         |     لا فعليًا | لا يوجد نظام خارجي بعد               |
| (`RAG`)                  |            لا | لا توجد قاعدة معرفة بعد              |
| (`LangSmith`)            |            لا | لم نربط التتبع بعد                   |
| (`Handoff`)              |            لا | لا يوجد موظف يستلم المحادثة          |
| (`Skills`)               |            لا | لا توجد تعليمات تخصصية مستقلة حاليًا |
| (`Supervisor/Subagents`) |            لا | غير مبرر حتى الآن                    |
| (`Subgraphs`)            |            لا | تدفقات الأقسام لم تصبح كبيرة بعد     |

## متى سنستخدم الأنماط الأخرى؟

* نستخدم (`RAG`) عندما ندخل ملفات خدمات وسياسات الشركة.
* نستخدم (`Tool Calling`) عند الاتصال بنظام تذاكر أو بريد أو (`CRM`).
* نستخدم (`Handoff`) عندما يستلم موظف حقيقي المحادثة.
* نستخدم (`Subgraph`) إذا أصبح لقسم مثل (`CYBTX`) مسار طويل مستقل.
* نستخدم (`Subagents`) عندما يمتلك كل قسم أدوات وبيانات وصلاحيات مختلفة.
* نستخدم (`Supervisor`) فقط للطلبات متعددة الأقسام التي تحتاج تنسيقًا حقيقيًا.
* نستخدم (`Skills`) لتعليمات تخصصية قابلة لإعادة الاستخدام دون إنشاء وكيل جديد.

## خطة المراحل التالية

### المرحلة التالية مباشرة: المراقبة والتقييم

سنضيف (`LangSmith`) لنعرف:

* ماذا أرسلنا للنموذج.
* ماذا أعاد النموذج.
* أي عقدة عملت.
* زمن وتكلفة كل استدعاء.
* لماذا فشل مثال معين.

وسنجهز مجموعة تقييم صغيرة لعبارات حقيقية مثل:

```text
أشتي أحمي موقعي لأنهم اخترقوني
أشتي العربي فقط
كم بيكلف الموقع؟
ما نتيجة المباراة؟
أريد إضافة كتاب كميزة
```

هذه الخطوة مهمة لأننا كنا نصلح سلوك النموذج دون رؤية كاملة للتتبع.

### بعدها

1. تخزين دائم باستخدام (`PostgreSQL`).
2. عدادات التكرار والإيقاف والتحقق البشري.
3. قاعدة معرفة خدمات الشركة (`RAG`).
4. ربط نظام التذاكر الحقيقي (`Tool Calling / Adapter`).
5. متابعة الحالة والإشعارات.
6. التحويل إلى موظف (`Handoff`).
7. واجهة المحادثة والمصادقة والنشر.
8. تقييم الحاجة إلى (`Subgraphs/Subagents`) حسب تعقيد الأقسام.

الخلاصة: **النواة الذكية للمحادثة والتذكرة تعمل، لكن ما زلنا نملك نظامًا تجريبيًا مخزنًا في الذاكرة، وليس نظام شركة إنتاجيًا بعد. والخطوة الصحيحة الآن هي (`LangSmith Tracing + Evaluation`) قبل توسيع النظام.**
